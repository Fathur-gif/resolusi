node {
  stage('SCM') {
    checkout scm
  }
stage ("Code Quality") {
    steps {
      withSonarQubeEnv("test1") {
        sh "npm install sonar-scanner"
        sh "npm run sonar"
          }
      }
  }
