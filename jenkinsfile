node {
  stage('SCM') {
    checkout scm
  }
stage ("Code Quality") {
            steps {
                withSonarQubeEnv("SonarQube") {
                    sh "npm install sonar-scanner"
                    sh "npm run sonar"
                }
            }
        }
